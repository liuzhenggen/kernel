FILE(GLOB TEST_SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/*.c
)

FOREACH(test ${TEST_SOURCES})
    STRING(REGEX REPLACE "${CMAKE_CURRENT_SOURCE_DIR}/(.*)\\.c" "\\1" TEMP_EXECUTE ${test})
    ADD_EXECUTABLE(${TEMP_EXECUTE} ${test})
ENDFOREACH()